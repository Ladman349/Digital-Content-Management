# This file is committed, so it must only ever contain non-secret values.
# The API URL below is public information.
#
# Do NOT put VITE_ADMIN_API_KEY here, or anywhere else now that people sign in with their own
# accounts: a key in the bundle would bypass sign-in for everyone (DEPLOYMENT.md, Section 6).
VITE_API_URL=https://digital-content-management-production-9a88.up.railway.app
