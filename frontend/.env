# This file is committed, so it must only ever contain non-secret values.
# The API URL below is public information.
#
# Do NOT put VITE_ADMIN_API_KEY here. Set it in the Vercel project's environment
# variables, or locally in .env.local, which is git-ignored.
VITE_API_URL=https://digital-content-management-production-6fd4.up.railway.app
